var order = function (words) {
    var arr = words.split(" ");
    var obj = {};
    arr.forEach(function (it) { return (it.match(/\d+/).input); });
    return arr.join();
};
var x = order('is2 Thi1s T4est 3a');
console.log(x);
